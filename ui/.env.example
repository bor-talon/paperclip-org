# Clerk publishable key — enables Clerk auth UI (get from Clerk Dashboard)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx

# Backend API URL — only needed when UI is deployed separately from the server
# VITE_API_URL=https://your-backend.example.com/api
